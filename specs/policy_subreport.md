# 請幫我設計 `policy_details_subreport.jrxml`

## 問題

- 我現在的 `policy_list.jrxml` 無法看到詳細的保單資訊

## 我的需求

- 新增一個 `policy_details.jrxml`:
  - 設計同 `policy_list.jrxml`。
  - `policy_details.jrxml` 的 detail 區會把資料傳給 `policy_details_subreport.jrxml`
- data.sql、schema.sql 需要增加一個 policy_details
